namespace PingRunner.App.ViewModels;

/// <summary>How a figure reads at a glance; views color it with the matching status brush.</summary>
public enum Severity
{
    Neutral,
    Good,
    Warning,
    Bad,
}
